package com.arkamo.rickandmorty.designsystem.foundation.color.token

/**
 * Brand-specific colors, used as primary colors in the UI.
 * - primary: the main color
 * - highlight: color to highlight secondary actions or elements
 */
data class BrandColors(
    val primary: ColorScale,
    val highlight: ColorScale,
)
